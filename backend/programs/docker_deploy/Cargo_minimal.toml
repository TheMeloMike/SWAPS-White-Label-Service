[package]
name = "swaps-contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
path = "minimal_swaps_contract.rs"

[features]
no-entrypoint = []
default = []

[dependencies]
solana-program = "1.14"